// Select the root node (the <html> element)
const rootNode = document.documentElement;

// Select the last child of the root node (the <h2> element)
const lastChildOfRoot = rootNode.lastElementChild;

// Select all the children of the body element
const bodyChildren = document.body.children;

// Select the next sibling of the h1 node (the <ul> element)
const h1NextSibling = document.querySelector('h1').nextElementSibling;

// Select the parent element of the h1 node (the <body> element)
const h1ParentElement = document.querySelector('h1').parentElement;
